public class Solution {
    // you need to treat n as an unsigned value
    public int hammingWeight(int n) {
        int count=0;
       while(n!=0)
        { if((n&1)==1) //the ()for n&1 is necessary
          count++;
          n >>>=1; //there must be a =
            
        }
        return count;
    }
}
